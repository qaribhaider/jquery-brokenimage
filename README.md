# jQuery Broken Image

Provides support to add fallback images for broken images

## Usage

Include the script **jquery.brokenimage.js** in the html page and bind to image elements using

```sh
$('img').qImageFallback();
```

Check **index.html** for example/demo
